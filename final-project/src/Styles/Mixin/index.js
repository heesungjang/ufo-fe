import {
    flexBox,
    floatBox,
    outline,
    boxShadow,
    darkBoxShadow,
} from "./boxStyle";
import { textProps, textOverflow, textboxOverflow } from "./textStyle";

const mixin = {
    flexBox,
    floatBox,
    outline,
    boxShadow,
    darkBoxShadow,
    textProps,
    textOverflow,
    textboxOverflow,
};

export default mixin;
