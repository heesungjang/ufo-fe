import React from "react";
import Clock2 from "./Clock2";
const Count2 = ({ deadline }) => {
    return (
        <>
            <Clock2 deadline={deadline} />
        </>
    );
};

export default Count2;
